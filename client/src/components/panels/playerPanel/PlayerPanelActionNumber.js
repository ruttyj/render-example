import React from "react";

export default ({ children }) => {
  return (
    <div
      style={{
        textAlign: "center",
        fontSize: "30px"
      }}
    >
      {children}
    </div>
  );
};
